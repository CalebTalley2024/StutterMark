# Adapters convert different datasets' raw formats into the same Example(user, assistant, kind).
# So format/trigger logic stays dataset-agnostic; swap datasets by adding an adapter + registry entry.
