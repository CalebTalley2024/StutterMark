[project]
name = "stuttermark"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Caleb Talley", email = "calebtalley2024@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0.3",
]

[build-system]
requires = ["uv_build>=0.10.2,<0.11.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
